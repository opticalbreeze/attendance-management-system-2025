# 🌐 ネットワーク設定ガイド

サーバー用PCのIPアドレスを固定する方法を説明します。

---

## 📋 目次

1. [WindowsでのIPアドレス固定方法](#windowsでのipアドレス固定方法)
2. [現在のIPアドレス確認方法](#現在のipアドレス確認方法)
3. [固定IPアドレスの設定手順](#固定ipアドレスの設定手順)
4. [設定確認とトラブルシューティング](#設定確認とトラブルシューティング)

---

## 🪟 WindowsでのIPアドレス固定方法

### 前提条件

- Windows管理者権限が必要です
- ネットワーク管理者に相談して、使用可能なIPアドレス範囲を確認してください

---

## 🔍 現在のIPアドレス確認方法

### 方法1: コマンドプロンプトで確認（推奨）

**コマンドプロンプトまたはPowerShellを開く:**
- **Windowsキー + R** → `cmd` と入力 → Enter
- または、**Windowsキー + X** → **Windows PowerShell** または **ターミナル**

**コマンドを実行:**
```cmd
ipconfig
```

**詳細情報を確認する場合:**
```cmd
ipconfig /all
```

**出力例:**
```
イーサネット アダプター イーサネット:

   接続固有の DNS サフィックス . . . :
   リンクローカル IPv6 アドレス . . . . : fe80::xxxx:xxxx:xxxx:xxxx%xx
   IPv4 アドレス . . . . . . . . . . . : 192.168.1.100  ← これが現在のIPアドレス
   サブネット マスク . . . . . . . . . : 255.255.255.0  ← サブネットマスク
   デフォルト ゲートウェイ . . . . . . : 192.168.1.1    ← デフォルトゲートウェイ
```

**重要な情報:**
- **IPv4アドレス**: 固定したいIPアドレス（例：`192.168.1.100`）
- **サブネットマスク**: 通常は `255.255.255.0`
- **デフォルトゲートウェイ**: ルーターのIPアドレス（例：`192.168.1.1`）
- **DNSサーバー**: `ipconfig /all` で確認（例：`192.168.1.1`）

### 方法2: 設定画面で確認

1. **設定**（Windowsキー + I）を開く
2. **ネットワークとインターネット** → **イーサネット**（または**Wi-Fi**）
3. 接続しているネットワーク名をクリック
4. **プロパティ**セクションでIPアドレスを確認

---

## ⚙️ 固定IPアドレスの設定手順（Windows）

### 📋 事前準備：現在のネットワーク情報を確認

まず、現在のIPアドレス情報を確認します：

**コマンドプロンプトまたはPowerShellで実行:**
```cmd
ipconfig /all
```

**確認する項目:**
- IPv4アドレス（例：`192.168.1.100`）
- サブネットマスク（例：`255.255.255.0`）
- デフォルトゲートウェイ（例：`192.168.1.1`）
- DNSサーバー（例：`192.168.1.1`）

### 手順1: ネットワーク接続の設定画面を開く

**方法A: コマンドで開く（推奨）**
1. **Windowsキー + R** を押す
2. `ncpa.cpl` と入力して **Enter** キーを押す

**方法B: 設定から開く**
1. **設定**（Windowsキー + I）を開く
2. **ネットワークとインターネット** → **アダプターのオプション** をクリック

**方法C: コントロールパネルから開く**
1. **コントロールパネル** → **ネットワークと共有センター**
2. 左側の **アダプターの設定の変更** をクリック

### 手順2: 使用中のネットワークアダプターを特定

**ネットワーク接続** ウィンドウで、以下のアイコンを確認：

- **有線接続**: 「イーサネット」または「ローカルエリア接続」
- **無線接続**: 「Wi-Fi」または「ワイヤレスネットワーク接続」

**接続状態の確認:**
- アイコンに「×」がない = 接続中
- アイコンに「×」がある = 切断中（別のアダプターを選択）

### 手順3: ネットワークアダプターのプロパティを開く

1. 使用中のネットワークアダプターを **右クリック**
2. **プロパティ** を選択
3. 管理者権限の確認ダイアログが表示されたら **続行** をクリック

### 手順4: IPv4の設定を変更

1. **この接続は次の項目を使用します** の一覧から
   **☑ インターネット プロトコル バージョン 4 (TCP/IPv4)** を探す
2. **インターネット プロトコル バージョン 4 (TCP/IPv4)** を選択（クリック）
3. **プロパティ** ボタンをクリック

### 手順5: IPアドレスを固定設定に変更

1. **次のIPアドレスを使う** を選択（ラジオボタン）
2. 以下の項目を入力：

**入力例（実際の値は `ipconfig /all` で確認した値を使用）:**

| 項目 | 入力例 | 説明 |
|------|--------|------|
| IPアドレス | `192.168.1.100` | 現在のIPアドレスをそのまま使用、または空いているIPアドレス |
| サブネットマスク | `255.255.255.0` | 現在のサブネットマスクと同じ値 |
| デフォルトゲートウェイ | `192.168.1.1` | 現在のデフォルトゲートウェイと同じ値 |

### 手順6: DNSサーバーの設定

**方法A: 自動取得（推奨）**
- **次のDNSサーバーのアドレスを自動的に取得する** を選択

**方法B: 手動設定**
- **次のDNSサーバーのアドレスを使う** を選択
- 以下の値を入力：

```
優先DNSサーバー:   192.168.1.1  （ルーターのIPアドレス、または現在のDNSサーバー）
代替DNSサーバー:   8.8.8.8      （Google DNS、任意）
```

### 手順7: 設定を適用

1. **OK** ボタンをクリック
2. **プロパティ** ウィンドウでも **OK** をクリック
3. すべてのウィンドウを閉じる
4. ネットワーク接続が一時的に切断される場合があります（数秒で自動復旧）

### 手順8: 設定の確認

**コマンドプロンプトまたはPowerShellで確認:**
```cmd
ipconfig
```

**確認ポイント:**
- IPv4アドレスが設定した値になっているか
- デフォルトゲートウェイが正しく設定されているか
- インターネットに接続できるか（`ping 8.8.8.8`）

---

## ✅ 設定確認とトラブルシューティング

### 設定確認

```powershell
# IPアドレスが正しく設定されているか確認
ipconfig

# ネットワーク接続をテスト
ping 8.8.8.8

# デフォルトゲートウェイに接続できるか確認
ping 192.168.1.1
```

### トラブルシューティング

#### 問題1: インターネットに接続できない

**原因:** IPアドレスが他のデバイスと重複している可能性

**解決策:**
1. 別のIPアドレスを試す（例：192.168.1.101）
2. ネットワーク管理者に確認して、使用可能なIPアドレス範囲を確認

#### 問題2: ネットワークに接続できない

**原因:** サブネットマスクまたはデフォルトゲートウェイが間違っている

**解決策:**
1. `ipconfig /all` で現在の設定を確認
2. 同じネットワーク内の他のPCの設定を参考にする
3. ルーターの管理画面で設定を確認

#### 問題3: DNSが解決できない

**原因:** DNSサーバーの設定が間違っている

**解決策:**
1. **DNSサーバーのアドレスを自動的に取得する** を選択
2. または、ルーターのIPアドレスをDNSサーバーとして設定

---

## 📝 設定例

### 例1: 一般的な家庭用ルーター環境

```
IPアドレス:         192.168.1.100
サブネットマスク:    255.255.255.0
デフォルトゲートウェイ: 192.168.1.1
優先DNSサーバー:     192.168.1.1
代替DNSサーバー:     8.8.8.8
```

### 例2: オフィス環境

```
IPアドレス:         192.168.10.50
サブネットマスク:    255.255.255.0
デフォルトゲートウェイ: 192.168.10.1
優先DNSサーバー:     192.168.10.1
代替DNSサーバー:     8.8.8.8
```

**注意:** オフィス環境では、ネットワーク管理者に相談してから設定してください。

---

## 🔒 セキュリティに関する注意事項

1. **IPアドレスを固定する前に:**
   - ネットワーク管理者に相談
   - 使用可能なIPアドレス範囲を確認
   - 他のデバイスと重複しないように注意

2. **固定IPアドレスのメリット:**
   - サーバーに常に同じIPアドレスでアクセス可能
   - ポートフォワーディング設定が簡単
   - ネットワーク設定が安定

3. **固定IPアドレスのデメリット:**
   - ネットワーク環境が変わると設定変更が必要
   - 手動設定のため、設定ミスのリスクがある

---

## 🔗 関連ドキュメント

- [README.md](./README.md) - プロジェクト全体の概要
- [DOCKER_GUIDE.md](./DOCKER_GUIDE.md) - Docker環境構築ガイド
- [TROUBLESHOOTING.md](./TROUBLESHOOTING.md) - トラブルシューティング

---

## 💡 補足情報

### DHCPと固定IPの違い

- **DHCP（自動取得）**: ルーターが自動的にIPアドレスを割り当てる
  - メリット: 設定が簡単、ネットワーク変更に自動対応
  - デメリット: IPアドレスが変わる可能性がある

- **固定IP（手動設定）**: 手動でIPアドレスを設定する
  - メリット: IPアドレスが変わらない、サーバー運用に適している
  - デメリット: 設定が必要、ネットワーク変更時に手動対応が必要

### サーバー運用での推奨設定

サーバー用PCでは、**固定IPアドレス**の使用を推奨します：

1. クライアントPCから常に同じIPアドレスでアクセス可能
2. ファイアウォール設定が簡単
3. ネットワーク設定が安定

---

**更新日**: 2025-11-19


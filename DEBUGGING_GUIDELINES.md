# デバッグ・問題解決ガイドライン

## ⚠️ 重要な戒め

**このドキュメントは、ユーザーの要望が通らない限り、作業開始前に必ず読み返すこと。**

## 根本的な問題を見落とさないための原則

### 1. 最初に必ず行うこと

**問題が発生したら、まず以下を確認すること：**

1. **ファイル構造の比較**
   - 動作しているページと動作していないページの**HTML構造**を比較
   - **JavaScriptの読み込み方法**（インライン vs 外部ファイル）を確認
   - **CSSの読み込み方法**を確認
   - ファイル全体の構造を把握してから変更を加える

2. **実際の動作確認**
   - ブラウザの開発者ツールで実際に読み込まれているファイルを確認
   - コンソールエラーを確認
   - ネットワークタブでファイルの読み込み状況を確認

3. **コードの実装比較**
   - 動作しているコードと動作していないコードを**行ごとに比較**
   - 関数名、変数名、クラス名の違いを確認
   - 実行順序やタイミングの違いを確認

### 2. 表面的な変更を避ける

**以下のような表面的な変更は、根本原因を解決しない限り無意味：**

- ❌ バージョン番号を変更するだけ（`?v=20251210_15` → `?v=20251210_16`）
- ❌ デバッグログを追加するだけ
- ❌ CSSクラス名を変更するだけ
- ❌ 関数の実装を変更するが、読み込み方法の違いを無視する

**代わりに：**
- ✅ ファイル構造の違いを特定する
- ✅ 読み込み方法の違いを確認する
- ✅ ブラウザキャッシュの問題か、コードの問題かを切り分ける
- ✅ 動作しているページと同じ構造にする

### 3. 今回の問題の教訓（2025-12-10）

**問題：** `/attendance-check`ページで色分けが動作しない

**間違ったアプローチ：**
1. `formatAttendanceTimes`関数の実装を変更
2. `getValidClockTimes`関数の実装を変更
3. CSSクラス名を変更
4. バージョン番号を何度も変更
5. デバッグログを追加

**正しいアプローチ（後から判明）：**
1. `/search`ページと`/attendance-check`ページの**HTML構造を比較**
2. `/search`は**インラインJS**、`/attendance-check`は**外部JSファイル**を読み込んでいることを発見
3. 外部JSファイルはブラウザキャッシュの問題で更新されていない可能性が高い
4. `/testpage`を作成してインラインJSで動作確認 → 動作した
5. **根本原因：外部JSファイルのキャッシュ問題**

**学んだこと：**
- 最初にファイル構造を比較していれば、すぐに原因を特定できた
- 表面的な変更（バージョン番号変更、デバッグログ追加）は時間の無駄
- 動作しているページと同じ構造にすることが最速の解決策

### 4. チェックリスト

**問題が発生したら、以下を順番に確認：**

- [ ] 動作しているページと動作していないページのHTML構造を比較したか？
- [ ] JavaScriptの読み込み方法（インライン vs 外部ファイル）を確認したか？
- [ ] CSSの読み込み方法を確認したか？
- [ ] ブラウザの開発者ツールで実際に読み込まれているファイルを確認したか？
- [ ] コンソールエラーを確認したか？
- [ ] ネットワークタブでファイルの読み込み状況を確認したか？
- [ ] 動作しているページと同じ構造にできるか検討したか？
- [ ] 表面的な変更（バージョン番号変更だけなど）をしていないか？

### 5. 作業フロー

1. **問題の報告を受ける**
2. **このドキュメントを読む** ← 重要
3. **動作しているページと動作していないページを比較**
4. **ファイル構造の違いを特定**
5. **根本原因を特定**
6. **最小限の変更で解決策を実装**
7. **動作確認**

### 6. 繰り返し読むべきタイミング

- ユーザーから「動作しない」「変化がない」という報告を受けた時
- 複数回同じような変更をしている時
- 表面的な変更（バージョン番号変更、デバッグログ追加）をしようとしている時
- 根本原因がわからない時

---

**最後に：**

このドキュメントは、表面的な変更を繰り返し、ユーザーの時間とトークンを無駄にしないために作成されました。

**ユーザーの要望が通らない限り、このドキュメントを繰り返し読むこと。**


# デバッグ・問題解決ガイドライン

## ⚠️ 重要な戒め

**このドキュメントは、ユーザーの要望が通らない限り、作業開始前に必ず読み返すこと。**

## 事前検証システム（問題発生前の予防）

### 1. タスク受理時の事前リスク評価
**問題が発生する前に以下を確認：**
- [ ] ブラウザキャッシュ問題のリスク → インライン実装優先
- [ ] 外部ファイル vs インライン実装 → 既存パターン優先
- [ ] データベーススキーマ変更 → 影響範囲事前確認
- [ ] APIレスポンス構造の不一致 → 他のAPIでの使用例確認

### 2. 成功パターンの積極的活用
**既知の動作パターンの強制優先：**
- 成功したAPI構造の再利用
- 動作確認済みCSS/JavaScript パターンの適用
- 安定したファイル構造の踏襲

### 3. 高速問題解決テンプレート（5分以内目標）
```
1. (30秒) semantic_searchで動作している類似ページを特定
2. (60秒) read_fileでそのHTMLとJSの構造を確認 
3. (90秒) grep_searchで問題のあるページとの差異を特定
4. (120秒) 最小限の変更で修正実装
5. (30秒) read_fileで動作確認
```

## 根本的な問題を見落とさないための原則

### 1. 最初に必ず行うこと

**問題が発生したら、まず以下を確認すること：**

1. **ファイル構造の比較**
   - 動作しているページと動作していないページの**HTML構造**を比較
   - **JavaScriptの読み込み方法**（インライン vs 外部ファイル）を確認
   - **CSSの読み込み方法**を確認
   - ファイル全体の構造を把握してから変更を加える

2. **実際の動作確認**
   - ブラウザの開発者ツールで実際に読み込まれているファイルを確認
   - コンソールエラーを確認
   - ネットワークタブでファイルの読み込み状況を確認

3. **コードの実装比較**
   - 動作しているコードと動作していないコードを**行ごとに比較**
   - 関数名、変数名、クラス名の違いを確認
   - 実行順序やタイミングの違いを確認

### 2. 表面的な変更を避ける

**以下のような表面的な変更は、根本原因を解決しない限り無意味：**

- ❌ バージョン番号を変更するだけ（`?v=20251210_15` → `?v=20251210_16`）
- ❌ デバッグログを追加するだけ
- ❌ CSSクラス名を変更するだけ
- ❌ 関数の実装を変更するが、読み込み方法の違いを無視する

**代わりに：**
- ✅ ファイル構造の違いを特定する
- ✅ 読み込み方法の違いを確認する
- ✅ ブラウザキャッシュの問題か、コードの問題かを切り分ける
- ✅ 動作しているページと同じ構造にする

### 3. 今回の問題の教訓（2025-12-10）

**問題：** `/attendance-check`ページで色分けが動作しない

**間違ったアプローチ：**
1. `formatAttendanceTimes`関数の実装を変更
2. `getValidClockTimes`関数の実装を変更
3. CSSクラス名を変更
4. バージョン番号を何度も変更
5. デバッグログを追加

**正しいアプローチ（後から判明）：**
1. `/search`ページと`/attendance-check`ページの**HTML構造を比較**
2. `/search`は**インラインJS**、`/attendance-check`は**外部JSファイル**を読み込んでいることを発見
3. 外部JSファイルはブラウザキャッシュの問題で更新されていない可能性が高い
4. `/testpage`を作成してインラインJSで動作確認 → 動作した
5. **根本原因：外部JSファイルのキャッシュ問題**

**学んだこと：**
- 最初にファイル構造を比較していれば、すぐに原因を特定できた
- 表面的な変更（バージョン番号変更、デバッグログ追加）は時間の無駄
- 動作しているページと同じ構造にすることが最速の解決策

### 3-2. APIレスポンス構造の問題の教訓（2025-12-12）

**問題：** チェック済みデータがデータベースに存在するが、フロントエンドで`result.data`が`undefined`になる

**間違ったアプローチ：**
1. デバッグログを大量に追加
2. `docker-compose.yml`にファイルマウントを追加
3. `constants.py`のマウントを追加
4. 表面的な修正を繰り返す
5. **ガイドラインを読んだが、原則に従わなかった**

**正しいアプローチ（後から判明）：**
1. **データベースにデータがあることを確認**（実施済み）
2. **APIレスポンスの構造を確認** - `format_response`関数の動作を理解
3. **他のAPIエンドポイントでの`format_response`の使用例を確認**
4. **データフローを追跡** - データベース → API → フロントエンド
5. **根本原因：`format_response`関数が辞書を直接マージするため、`response['data']`が存在しない**

**学んだこと：**
- **ガイドラインを読んだだけでは不十分。原則に従うことが重要**
- **動作しているコードと動作していないコードを行ごとに比較する**べきだった
- **データフローを追跡する**べきだった（データベース → API → フロントエンド）
- **表面的な変更（デバッグログ追加、ファイルマウント追加）は時間の無駄**
- **根本原因を特定してから修正する**べきだった

**反省点：**
- ユーザーが「ガイドラインを読み直して再検討願う」と指示したのに、従わなかった
- デバッグログを追加するだけの表面的な変更を繰り返した
- `format_response`関数の動作を理解せずに修正を試みた
- 他のAPIエンドポイントでの使用例を確認しなかった

### 4. ハイパー効率チェックリスト（強化版）

**問題が発生したら、以下を順番に確認：**

### 段階1: 事前検証（必須）
- [ ] semantic_searchで類似実装を検索したか？
- [ ] 動作しているパターンを特定したか？
- [ ] RED FLAGが検出されていないか？

### 段階2: 構造比較（最優先）
- [ ] 動作しているページと動作していないページのHTML構造を比較したか？
- [ ] JavaScriptの読み込み方法（インライン vs 外部ファイル）を確認したか？
- [ ] CSSの読み込み方法を確認したか？

### 段階3: 動作確認（可能な範囲で）
- [ ] ブラウザの開発者ツールで実際に読み込まれているファイルを確認したか？
- [ ] コンソールエラーを確認したか？
- [ ] ネットワークタブでファイルの読み込み状況を確認したか？

### 段階4: コード比較（最重要）
- [ ] 動作しているコードと動作していないコードを行ごとに比較したか？
- [ ] データフローを追跡したか？（データベース → API → フロントエンド）
- [ ] APIレスポンスの構造を確認したか？（関数の動作を理解したか？）
- [ ] 他のエンドポイントでの同じ関数の使用例を確認したか？

### 段階5: 根本原因特定と解決
- [ ] 動作しているページと同じ構造にできるか検討したか？
- [ ] 表面的な変更（バージョン番号変更だけなど）をしていないか？
- [ ] 根本原因を特定してから修正したか？
- [ ] メタ認知チェックを実行したか？

### 4-1. 「ガイドラインに従ってチェックを実行せよ」という指示があった場合の必須手順

**⚠️ 最重要：ユーザーから「ガイドラインに従ってチェックを実行せよ」という指示があった場合、エージェントはチェックリストの各項目を実際に実行し、その結果を必ずユーザーに出力すること。**

**チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされる。**

**出力フォーマット：**
```
## ガイドライン遵守チェック結果

### DEBUGGING_GUIDELINES.mdのチェックリスト実行結果

- [ ] 動作しているページと動作していないページのHTML構造を比較したか？
  結果: [実際に確認した結果を記載。例: "read_fileでsearch.htmlとattendance_check.htmlを比較。search.htmlはインラインJS、attendance_check.htmlは外部JSファイルを使用"]
  確認方法: [使用した方法を記載。例: "read_file work_attend_server/server/templates/search.html; read_file work_attend_server/server/templates/attendance_check.html"]

- [ ] JavaScriptの読み込み方法（インライン vs 外部ファイル）を確認したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] CSSの読み込み方法を確認したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] ブラウザの開発者ツールで実際に読み込まれているファイルを確認したか？
  結果: [実際に確認した結果を記載。確認できない場合は「ブラウザの開発者ツールは直接確認できないため、ファイル構造を確認した」と記載]
  確認方法: [使用した方法を記載]

- [ ] コンソールエラーを確認したか？
  結果: [実際に確認した結果を記載。確認できない場合は「コンソールエラーは直接確認できないため、コードの構文エラーを確認した」と記載]
  確認方法: [使用した方法を記載]

- [ ] ネットワークタブでファイルの読み込み状況を確認したか？
  結果: [実際に確認した結果を記載。確認できない場合は「ネットワークタブは直接確認できないため、ファイルの存在を確認した」と記載]
  確認方法: [使用した方法を記載]

- [ ] 動作しているページと同じ構造にできるか検討したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] 表面的な変更（バージョン番号変更だけなど）をしていないか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] データフローを追跡したか？（データベース → API → フロントエンド）
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] 動作しているコードと動作していないコードを行ごとに比較したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] APIレスポンスの構造を確認したか？（関数の動作を理解したか？）
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] 他のエンドポイントでの同じ関数の使用例を確認したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]

- [ ] 根本原因を特定してから修正したか？
  結果: [実際に確認した結果を記載]
  確認方法: [使用した方法を記載]
```

**重要：**
- **各チェック項目の「結果」には、実際に実行したコマンドの出力や、確認したファイルの行番号などを具体的に記載すること**
- **「確認しました」という曖昧な表現は禁止**
- **「grepで検索しました」ではなく、「grep -n 'PDF出力' monthly_report.html → No matches found」のように具体的な結果を記載すること**
- **確認できない項目がある場合は、「確認できない」と明記し、その理由を記載すること**

### 5. 作業フロー（高速実行版）

### ステップ1: 事前検証（必須 - 1分以内）
1. **タスク受理チェックを実行**
2. semantic_searchで類似機能を検索
3. 実装戦略を事前決定
4. RED FLAGがないことを確認

### ステップ2: 問題の報告を受ける（2分以内）
2. **このドキュメントを読む** ← 重要
3. **このドキュメントの原則に従う** ← 最重要（読んだだけでは不十分）
4. **動作しているページと動作していないページを比較**
5. **動作しているコードと動作していないコードを行ごとに比較**

### ステップ3: 高速解決（5分以内）
6. **データフローを追跡**（データベース → API → フロントエンド）
7. **APIレスポンスの構造を確認**（関数の動作を理解）
8. **他のエンドポイントでの同じ関数の使用例を確認**
9. **根本原因を特定**
10. **最小限の変更で解決策を実装**
11. **動作確認**

### メタ認知システム（自己監視機能）

**各ステップ後の強制自己チェック：**
```
□ "この変更で本当に問題が解決するか？"
□ "動作している類似コードと比較したか？"
□ "最小限の変更で済んでいるか？"
□ "検証可能な方法で確認したか？"
□ "既存のパターンを活用しているか？"
```

**エスカレーション判断：**
以下の場合は複雑な実装を避け、シンプルなアプローチに切り替え：
- 15分以上同じ問題で停滞
- 3つ以上のファイルを同時修正している
- 根本原因が特定できていない

**⚠️ 警告：**
- デバッグログを追加するだけでは解決しない
- ファイルマウントを追加するだけでは解決しない
- 表面的な変更を繰り返さない
- **ガイドラインを読んだだけでは不十分。原則に従うこと**

### 6. 繰り返し読むべきタイミング

- ユーザーから「動作しない」「変化がない」という報告を受けた時
- 複数回同じような変更をしている時
- 表面的な変更（バージョン番号変更、デバッグログ追加）をしようとしている時
- 根本原因がわからない時

### 7. エージェント作業強制実行手順書との連携

**重要：このガイドラインを読んだ後、必ず `AGENT_WORKFLOW_ENFORCEMENT.md` の手順に従うこと。**

`AGENT_WORKFLOW_ENFORCEMENT.md` には以下が記載されている：
- ファイル操作前の必須確認事項
- 「実装済み」と回答する前の必須チェック
- エディタの (unsaved) 状態の確認方法
- 実際のファイルを確認する方法
- 強制チェックポイント

**ガイドラインを読んだだけでは不十分。`AGENT_WORKFLOW_ENFORCEMENT.md` の手順を必ず実行すること。**

---

**最後に：**

このドキュメントは、表面的な変更を繰り返し、ユーザーの時間とトークンを無駄にしないために作成されました。

**ユーザーの要望が通らない限り、このドキュメントを繰り返し読むこと。**

**さらに、`AGENT_WORKFLOW_ENFORCEMENT.md` の手順を必ず実行すること。**

**⚠️ 最重要：「ガイドラインに従ってチェックを実行せよ」という指示があった場合、チェックリストの各項目を実際に実行し、その結果を必ずユーザーに出力すること。チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされる。**

## 更新履歴

- 2025-12-14: ガイドライン強化アップデート
  - 事前検証システムと予測的問題解決を追加
  - RED FLAG自動検出システムを導入
  - 高速実装テンプレート（5分以内目標）を追加
  - メタ認知システム（自己監視機能）を導入
  - 効率化優先順位システムを等級化
  - チェックリストを段階別に再構成（最優先順で実行）

- 2025-12-XX: チェックリスト結果出力の必須化
  - 「ガイドラインに従ってチェックを実行せよ」という指示があった場合、チェックリストの結果を必ず出力することを明記
  - チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされることを追加
  - 各チェック項目の結果に、実際に実行したコマンドの出力や確認したファイルの行番号などを具体的に記載することを必須化


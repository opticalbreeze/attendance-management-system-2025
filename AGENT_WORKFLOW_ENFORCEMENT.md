# エージェント作業強制実行手順書

## 目的
エージェントが指示を無視せず、確実に作業を完了させるための強制実行手順書。
トークン使用量を最小限に抑えながら最大の効果を得る。

## 事前検証システム（タスク受理前必須）

**タスク受理前に以下を必ず確認（5分以内完了目標）：**

### タスク受理チェック
- [ ] 必要なファイルがワークスペースに存在するか？
- [ ] 類似機能が既に実装済みでないか？（`semantic_search`で確認）
- [ ] 依存関係のあるファイル・機能は正常に動作しているか？
- [ ] 動作している類似ページ・機能を特定できるか？

### 実装戦略の事前決定
- **最優先**: 既存の動作しているコードのパターン特定と再利用
- **次優先**: 最小変更で目標達成する方法の決定
- **禁止**: 大幅なリファクタリングや新アーキテクチャ導入

### RED FLAG 即座検出システム
以下が検出されたら即座に戦略変更：
- 同じエラーが3回以上発生
- 10分以上同じ問題で停滞
- ファイル存在確認を3回以上実行
- 同一ファイルへの連続修正（3回以上）

**RED FLAG検出時の強制アクション：**
1. 現在のアプローチを中止
2. `semantic_search`で動作している類似機能を強制検索
3. そのコードパターンをベースに再実装

## 基本原則

### 1. ファイル操作前の必須確認事項

**エージェントは以下の順序で必ず実行すること：**

1. **エディタの状態確認**
   ```
   - ファイル名に (unsaved) が付いていないか確認
   - エディタの内容と実際のファイルが一致しているか確認
   ```

2. **実際のファイルの確認**
   ```bash
   # 必ず実行するコマンド
   findstr /n "検索文字列" ファイル名
   # または
   grep -n "検索文字列" ファイル名
   ```

3. **ワークスペースファイルの直接読み込み**
   ```
   read_file ツールで実際のファイルを読み込む
   grep ツールで実際のファイルを検索する
   ```

### 2. 「実装済み」と回答する前の必須チェック

**以下のすべてを確認してから回答すること：**

- [ ] エディタに `(unsaved)` が表示されていないか
- [ ] `grep` で実際のファイルに該当コードが存在するか
- [ ] `read_file` で実際のファイルの該当箇所を確認したか
- [ ] ワークスペースのファイルとエディタの内容が一致しているか

**1つでも未確認の項目があれば、「実装済み」と回答してはならない。**

### 3. ファイル編集時の必須手順

**ファイルを編集する際は必ず以下を実行：**

1. **編集前の確認**
   ```bash
   # 実際のファイルの該当箇所を読み込む
   read_file target_file 該当行の前後
   
   # 既存のコードを検索
   grep "検索文字列" target_file
   ```

2. **編集の実行**
   ```bash
   # search_replace または write ツールを使用
   # 必ず old_string が実際のファイルの内容と完全一致することを確認
   ```

3. **編集後の確認**
   ```bash
   # 編集後、必ず実際のファイルを確認
   read_file target_file 編集した箇所
   grep "追加したコード" target_file
   ```

### 4. ガイドライン違反の防止

**ガイドライン（DEBUGGING_GUIDELINES.md）を読んだ後、必ず以下を実行：**

1. **チェックリストの実行**
   ```
   - [ ] 動作しているページと動作していないページのHTML構造を比較したか？
   - [ ] JavaScriptの読み込み方法（インライン vs 外部ファイル）を確認したか？
   - [ ] ブラウザの開発者ツールで実際に読み込まれているファイルを確認したか？
   - [ ] 実際のファイルを直接確認したか？
   - [ ] エディタの (unsaved) 状態を確認したか？
   ```

2. **原則に従う**
   ```
   - 表面的な変更を避ける
   - 根本原因を特定してから修正する
   - 動作しているコードと動作していないコードを行ごとに比較する
   ```

### 4-1. 「ガイドラインに従ってチェックを実行せよ」という指示があった場合の必須手順

**⚠️ 最重要：ユーザーから「ガイドラインに従ってチェックを実行せよ」という指示があった場合、以下を必ず実行すること。**

1. **チェックリストの実行と結果の出力（必須）**

   **エージェントは、チェックリストの各項目を実際に実行し、その結果を必ずユーザーに出力すること。**

   **出力フォーマット：**
   ```
   ## ガイドライン遵守チェック結果
   
   ### ファイル操作前の必須確認事項
   - [ ] エディタに `(unsaved)` が表示されていないか
     結果: [実際に確認した結果を記載。例: "エディタを確認。ファイル名に(unsaved)は表示されていない"]
     確認方法: [使用したツールやコマンドを記載。例: "エディタの状態を確認"]
   
   - [ ] `grep` で実際のファイルに該当コードが存在するか
     結果: [実際に確認した結果を記載。例: "grepで'PDF出力'を検索。No matches found"]
     確認方法: [使用したコマンドを記載。例: "grep -n 'PDF出力' work_attend_server/server/templates/monthly_report.html"]
   
   - [ ] `read_file` で実際のファイルの該当箇所を確認したか
     結果: [実際に確認した結果を記載。例: "read_fileで240-260行目を確認。ボタンは存在しない"]
     確認方法: [使用したコマンドを記載。例: "read_file work_attend_server/server/templates/monthly_report.html offset=240 limit=20"]
   
   - [ ] ワークスペースのファイルとエディタの内容が一致しているか
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   ### DEBUGGING_GUIDELINES.mdのチェックリスト
   - [ ] 動作しているページと動作していないページのHTML構造を比較したか？
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   - [ ] JavaScriptの読み込み方法（インライン vs 外部ファイル）を確認したか？
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   - [ ] ブラウザの開発者ツールで実際に読み込まれているファイルを確認したか？
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   - [ ] 実際のファイルを直接確認したか？
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   - [ ] エディタの (unsaved) 状態を確認したか？
     結果: [実際に確認した結果を記載]
     確認方法: [使用した方法を記載]
   
   ### 強制チェックポイント
   - [ ] エディタに (unsaved) が表示されていないか？
     結果: [実際に確認した結果を記載]
   
   - [ ] 実際のファイルを read_file で確認したか？
     結果: [実際に確認した結果を記載]
   
   - [ ] grep で該当コードの存在を確認したか？
     結果: [実際に確認した結果を記載]
   
   - [ ] 編集後、実際のファイルを確認したか？
     結果: [実際に確認した結果を記載]
   
   - [ ] ガイドラインのチェックリストを実行したか？
     結果: [実際に確認した結果を記載]
   ```

2. **チェックリストの結果を出力しない場合の扱い**
   - **チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされる**
   - **ユーザーは、チェックリストの結果を出力することで、エージェントが適当にチェックを実行していないか判断できる**
   - **チェックリストの結果を出力せずに「確認しました」と回答することは禁止**

3. **チェックリストの結果の検証可能性**
   - **各チェック項目の「結果」には、実際に実行したコマンドの出力や、確認したファイルの行番号などを具体的に記載すること**
   - **「確認しました」という曖昧な表現は禁止**
   - **「grepで検索しました」ではなく、「grep -n 'PDF出力' monthly_report.html → No matches found」のように具体的な結果を記載すること**

### 5. エラー発生時の強制手順

**エラーが発生した場合、以下を必ず実行：**

1. **エラーメッセージの確認**
   ```
   - search_replace が失敗した場合、old_string が実際のファイルと一致しているか確認
   - read_file で実際のファイルの該当箇所を再確認
   ```

2. **実際のファイルの再確認**
   ```bash
   # 必ず実行
   read_file target_file エラーが発生した箇所の前後20行
   grep "関連する文字列" target_file
   ```

3. **修正の再実行**
   ```
   - 実際のファイルの内容を確認してから修正
   - 修正後、必ず read_file で確認
   ```

## 段階的実装強制システム

**すべての実装は以下の段階を必ず順守（段階飛ばし禁止）：**

### 段階1: 最小動作確認（必須）
```
1. 最もシンプルな実装で動作確認
2. 既存の動作しているコードパターンを活用
3. 1つの変更で1つの動作確認
4. 複雑な機能は後回し
```

### 段階2: 機能拡張（段階1成功後のみ）
```
1. 基本機能が確認できた後の拡張
2. 段階1のコードベースを変更
3. 既存パターンからの逸脱は最小限
```

### 段階3: 最適化（段階2成功後のみ）
```
1. パフォーマンス改善
2. UI/UX向上
3. エラーハンドリング強化
```

## ハイパー効率化実装テンプレート

### 問題報告への対応（5分以内完了目標）
```
1. (30秒) semantic_searchで動作している類似ページを特定
2. (60秒) read_fileでそのHTMLとJSの構造を確認
3. (90秒) grep_searchで問題のあるページとの差異を特定
4. (120秒) 最小限の変更で修正実装
5. (30秒) read_fileで動作確認
```

### 新機能実装（10分以内完了目標）
```
1. (120秒) semantic_searchで既存の類似機能を検索・確認
2. (180秒) read_fileでそのコードをベースに実装
3. (120秒) 必要な調整を実施
4. (180秒) read_fileでテスト・動作確認
```

## 作業フロー（強制実行版）

### ステップ1: 事前検証（必須）
```
1. タスク受理チェックを実行
2. semantic_searchで類似機能を検索
3. 実装戦略を事前決定
4. RED FLAGがないことを確認
```

### ステップ2: 問題の理解
```
1. ユーザーの要求を正確に理解する
2. 関連するファイルを特定する
3. 現在の実装状況を確認する（実際のファイルで）
```

### ステップ2: 現状確認（必須）
```
1. エディタの状態を確認（(unsaved) がないか）
2. 実際のファイルを read_file で読み込む
3. grep で該当コードの存在を確認
4. 動作している類似ページと比較（該当する場合）
```

### ステップ3: 実装
```
1. 実際のファイルの内容を確認してから編集
2. search_replace または write を使用
3. old_string が実際のファイルと完全一致することを確認
```

### ステップ4: 確認（必須）
```
1. 編集後、read_file で実際のファイルを確認
2. grep で追加したコードが存在することを確認
3. エディタに (unsaved) が表示されていないことを確認
```

### ステップ5: 報告
```
1. 実装した内容を具体的に報告
2. 確認したファイルの行番号を明記
3. 次のステップ（サーバー再起動など）を明記
```

## 禁止事項

**以下を絶対に行ってはならない：**

1. ❌ エディタの内容だけを見て「実装済み」と回答する
2. ❌ `grep` で「No matches found」が出ているのに無視する
3. ❌ エディタに `(unsaved)` が表示されているのに気づかない
4. ❌ 実際のファイルを確認せずに編集する
5. ❌ 編集後、実際のファイルを確認しない
6. ❌ ガイドラインを読んだだけで、チェックリストを実行しない
7. ❌ 同じ間違いを繰り返す

## メタ認知強化システム

### 各ステップ後の強制自己チェック
**実装の各段階で以下を自問自答（必須）：**
```
□ "この変更で本当に問題が解決するか？"
□ "動作している類似コードと比較したか？"
□ "最小限の変更で済んでいるか？"
□ "検証可能な方法で確認したか？"
□ "既存のパターンを活用しているか？"
```

### エスカレーション判断（複雑な実装の回避）
**以下の場合は複雑な実装を避け、シンプルなアプローチに強制切り替え：**
```
□ 15分以上同じ問題で停滞
□ 3つ以上のファイルを同時修正している
□ 根本原因が特定できていない
□ RED FLAGが検出された
□ 段階的実装の原則から逸脱している
```

## 強制チェックポイント

**各作業の開始前に必ず以下を確認：**

```
□ 事前検証システムを実行したか？
□ semantic_searchで類似パターンを確認したか？
□ エディタに (unsaved) が表示されていないか？
□ 実際のファイルを read_file で確認したか？
□ grep で該当コードの存在を確認したか？
□ 編集後、実際のファイルを確認したか？
□ ガイドラインのチェックリストを実行したか？
□ メタ認知チェックを実行したか？
```

**すべてにチェックが入るまで作業を完了してはならない。**

**⚠️ 重要：「ガイドラインに従ってチェックを実行せよ」という指示があった場合、上記のチェックポイントの各項目について、実際に確認した結果を必ずユーザーに出力すること。チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされる。**

## 超効率化ルール（トークン使用量最小化）

### 1. 必須ツール使用パターン（自動検証ロジック）
**問題発生時の強制実行順序：**
```
1. semantic_search → 類似実装を検索
2. read_file → 動作している参考コードを確認
3. grep_search → 現在の実装状況を確認
4. 実装 → read_fileで確認 → 動作テスト
```

### 2. 長いファイル読み込みの制限
- 1000行を超えるファイルは必要箇所のみ読み込み
- 複数ファイル同時読み込み禁止
- `grep_search`でターゲット特定後の限定読み込み

### 3. 出力の簡潔化強制
- チェックリスト結果：結論のみ（詳細は要求時のみ）
- エラー確認：関連箇所のみ抽出
- 実装報告：変更点と結果のみ

### 4. 解決アプローチの優先順位
**最優先（必ず最初に試す）：**
1. 動作しているコードのコピー&少量修正
2. 既存ファイルパターンの踏襲
3. 最小限のファイル変更

**次優先（最優先が不可の場合のみ）：**
1. 新機能の段階的追加
2. 既存機能の拡張

**最後の手段（他の方法が全て失敗した場合のみ）：**
1. 大幅なリファクタリング
2. 新しいアーキテクチャの導入

### 5. 無駄な確認を避ける
   - 最初から実際のファイルを確認する
   - エディタの内容と実際のファイルを混同しない
   - semantic_searchで類似パターンを最初に確認

### 6. 確実な実装
   - 1回で正しく実装する
   - 動作しているパターンをベースにする
   - 何度も修正を繰り返さない

### 7. 明確な報告
   - 実装した内容を具体的に報告
   - 確認したファイルの行番号を明記
   - 使用したベースパターンを明記

## 使用例

### 悪い例（禁止）
```
ユーザー: PDF出力ボタンを追加して
エージェント: エディタを確認 → 「実装済みです」と回答
→ 実際にはファイルに保存されていない
```

### 良い例（必須）
```
ユーザー: PDF出力ボタンを追加して
エージェント:
1. read_file で実際のファイルを確認
2. grep で「PDF出力」を検索 → No matches found
3. 実際のファイルにボタンがないことを確認
4. search_replace でボタンを追加
5. read_file で追加を確認
6. grep で「PDF出力」を検索 → 見つかった
7. 「実装しました。240行目に追加しました」と報告
```

## 効果的なプロンプトテンプレートと強制ルール

### ⚠️ 重要：エージェントへの強制力発揮ルール

ユーザーが以下のプロンプトパターンを使用した場合、エージェントは**必ず**指定された手順に従わなければならない。

### 1. 問題解決の基本パターン

**プロンプト形式：**
```
【問題】[具体的な問題]
【期待動作】[期待される動作]
【現状】[現在の動作]
【確認事項】
- 原因を特定してから修正せよ
- 修正前に、修正内容と根拠を説明せよ
- 修正後、デバッグログで動作確認してから完了と報告せよ
- チェックリストの各項目の結果を出力せよ
```

**エージェントの必須対応：**
1. 原因を特定し、具体的に説明する
2. 修正案を提示し、なぜその修正で解決するか説明する
3. 修正を実装する
4. デバッグ出力を追加して動作確認する
5. 動作確認のログを添付してから完了と報告する
6. チェックリストの各項目の結果を出力する

**違反時の扱い：**
- 上記のいずれかが欠けている場合、「完了」と報告してはならない
- 動作確認のログがない場合、完了報告は無効とする

### 2. ロジック修正時の強制プロンプト

**プロンプト形式：**
```
【修正要求】[何を修正するか]
【現在のロジックの問題点】[具体的な問題]
【正しいロジック】[期待される動作]
【作業手順】
1. 現在のコードを読み、問題点を特定せよ
2. 問題点を具体的に説明せよ
3. 修正案を提示し、なぜその修正で解決するか説明せよ
4. 修正を実装せよ
5. デバッグ出力を追加して動作確認せよ
6. テストケースの結果を示せ
7. 完了報告は、動作確認のログを添付してから行え
```

**エージェントの必須対応：**
- 各ステップを順番に実行し、各ステップの結果を報告する
- ステップ7を満たさない場合、「完了」と報告してはならない

### 3. デバッグ時の強制プロンプト

**プロンプト形式：**
```
【問題】[発生している問題]
【デバッグ要求】
- デバッグ出力を追加して、[何を確認するか]を確認せよ
- ログの内容を解釈して、問題の原因を特定せよ
- 原因を特定してから修正案を提示せよ
- 修正後、同じデバッグ出力で動作確認せよ
```

**エージェントの必須対応：**
- デバッグ出力を追加する
- ログを解釈し、原因を特定する
- 原因を特定してから修正案を提示する
- 修正後、同じデバッグ出力で動作確認する

### 4. 段階的実装の強制プロンプト

**プロンプト形式：**
```
【実装要求】[実装内容]
【段階的実装】
ステップ1: [最初のステップ]を実装し、動作確認してから次へ進め
ステップ2: [次のステップ]を実装し、動作確認してから次へ進め
ステップ3: [最後のステップ]を実装し、全体の動作確認を行え

各ステップで、動作確認のログを添付してから次へ進め
```

**エージェントの必須対応：**
- 各ステップを順番に実行する
- 各ステップで動作確認のログを添付する
- 前のステップの動作確認が完了するまで、次のステップに進まない

### 5. 完了報告の強制条件

**プロンプト形式：**
```
【完了報告の条件】
以下のすべてを満たしてから「完了」と報告せよ：
- 修正内容を具体的に説明している
- 動作確認のログを添付している
- テストケースの結果を示している
- チェックリストの各項目の結果を出力している
- エッジケースの処理を説明している
```

**エージェントの必須対応：**
- 上記のすべての条件を満たすまで、「完了」と報告してはならない
- 条件が1つでも欠けている場合、完了報告は無効とする

### 6. 理解確認の強制プロンプト

**プロンプト形式：**
```
【確認要求】
- この問題の本質を説明せよ
- なぜ前回の修正が不十分だったか説明せよ
- 今回の修正で何が変わるか説明せよ
- このロジックが正しく動作する根拠を示せ
```

**エージェントの必須対応：**
- 各項目に対して具体的に説明する
- 「わかりません」「確認できません」などの曖昧な回答は禁止
- 説明できない場合は、コードを読み直してから回答する

### 7. テスト要求の強制プロンプト

**プロンプト形式：**
```
【テスト要求】
以下のテストケースで動作確認せよ：
1. [テストケース1] - 期待結果：[結果]
2. [テストケース2] - 期待結果：[結果]

各テストケースの結果をログで確認し、期待結果と一致することを確認せよ
```

**エージェントの必須対応：**
- 各テストケースの結果をログで確認する
- 期待結果と一致しない場合、原因を特定して修正する
- すべてのテストケースが期待結果と一致してから完了と報告する

### 8. 根本的な理解を確認する強制プロンプト

**プロンプト形式：**
```
【根本理解確認】
- このロジックがどう動作するか説明せよ
- なぜこの判定で正しいのか説明せよ
- この修正で何が変わるか説明せよ
- 開始時間から終了時間までの経過時間を正しく理解しているか確認せよ
```

**エージェントの必須対応：**
- ロジックの動作を具体的に説明する
- 判定が正しい理由を説明する
- 修正による変化を説明する
- 経過時間の概念を正しく理解していることを示す

### 9. 動作確認の強制ルール

**エージェントは以下を必ず実行すること：**

1. **修正前の確認**
   - 現在のコードを読み、問題点を特定する
   - 問題点を具体的に説明する

2. **修正の実装**
   - 修正案を提示し、なぜその修正で解決するか説明する
   - 修正を実装する

3. **修正後の確認**
   - デバッグ出力を追加する
   - 動作確認のログを取得する
   - ログを解釈し、期待通りの動作を確認する

4. **完了報告**
   - 動作確認のログを添付する
   - テストケースの結果を示す
   - チェックリストの各項目の結果を出力する

**違反時の扱い：**
- 動作確認のログがない場合、完了報告は無効とする
- テストケースの結果がない場合、完了報告は無効とする
- チェックリストの結果がない場合、完了報告は無効とする

### 10. 表面的な修正の禁止

**禁止事項：**
- コードを変更しただけで、動作確認をしない
- デバッグ出力を追加しただけで、ログを確認しない
- 「修正しました」と報告するだけで、動作確認のログを添付しない
- 問題の本質を理解せずに、表面的な修正を行う

**必須事項：**
- 問題の本質を理解してから修正する
- 修正後、必ず動作確認する
- 動作確認のログを添付してから完了と報告する
- 開始時間から終了時間までの経過時間を正しく理解する

## 更新履歴

- 2025-12-XX: 初版作成
  - エージェントが指示を無視しないための強制手順書として作成

- 2025-12-XX: チェックリスト結果出力の必須化
  - 「ガイドラインに従ってチェックを実行せよ」という指示があった場合、チェックリストの結果を必ず出力することを明記
  - チェックリストの結果を出力しない場合、エージェントはチェックを実行していないとみなされることを追加
  - 各チェック項目の結果に、実際に実行したコマンドの出力や確認したファイルの行番号などを具体的に記載することを必須化

- 2025-12-15: 効果的なプロンプトテンプレートと強制ルールの追加
  - ユーザーが効果的なプロンプトを使用した場合のエージェントの必須対応を明記
  - 動作確認のログを添付しない場合、完了報告は無効とすることを追加
  - 表面的な修正の禁止と、問題の本質を理解してから修正することを必須化
  - 開始時間から終了時間までの経過時間を正しく理解することを必須化

